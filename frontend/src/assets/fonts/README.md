# Natto 字体文件

尾花大輔 / 尾も白い **Natto demo**（包内文件名 `nattodemo-Regular.otf`）。

本机请使用：

**`frontend/src/assets/fonts/natto.otf`**

（已从下载包拷入。woff2 / ttf 可选。）

条款禁止复制、改字体文件、再分发、再销售。不要 `git add` 字体；仓库已 gitignore。商用可以，须遵守包内 `terms_of_use`。这是 demo，字距未调完；正式版预计 2027 年起发售。
