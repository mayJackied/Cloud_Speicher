# 复制为 deploy.env 后再改。deploy.env 不入库。
DEPLOY_HOST=8.130.215.175
DEPLOY_USER=root
DEPLOY_SSH_PORT=22
# 私钥文件路径。不要填密码；留空则用密码登录。
DEPLOY_SSH_KEY=
DEPLOY_WEB_ROOT=/var/www/cloud-speicher
# 前端和后端在同一台机时用环回，避免再绕公网/FRP。
BACKEND_UPSTREAM=127.0.0.1:8080
# 本机 80/443 已被 FRP 占用时用 8888。
DEPLOY_HTTP_PORT=8888
