host: 8.130.215.175

server:
  port: 8080
spring:
  application:
    name: claude_Speicher
  datasource:
    url: jdbc:mysql://${host}/cloud_disk?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: "RRFDTTVEcjx4iAw.mysql"

mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.example.demo.entity
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-value: 1
      logic-not-delete-value: 0

my:
  val:
    file:
      path: "./files"
      publicPath: "./files/public"
      recycleBinName: "recycle_bin"
      retainTime: 7 #Days

      uploadBufferSize: 5 #MB

      defaultExpireDuration: 24 #Hours

    signKey: "reisen"
    expire: 30 #Days
    url: ${host}