# 功能测试清单



最后一轮现网复测：**2026-09-04**  

账号：管理员 `Plato_777`（`userId = 8`，`isAdmin: true`）；本轮另注册普通用户 `umtm7rt4y`（`userId = 13`）  

模式：在线（Vite 代理 `8.130.215.175:8080`）+ 离线 mock 对照  

说明：勾选 = 该轮已测且通过；未勾选 = 失败 / 跳过 / 自动化受限。备注写清 `code` 与现象。



契约：`Result { code, data }`，成功 `code = 1`；请求头 `token`（除登录/注册/查用户名）。上传 `path` = **已存在的目标文件夹**；新建文件夹 `path` = **要创建的完整路径**。



---



## 0. 环境与连通



- [x] 切到「在线」（`apiMode = online`），右上角 pill 显示「稳定 / 在线」

- [x] 未登录 `POST /api/user/checkUserName` → HTTP 200，`is_available` 真假正确

- [x] 登录成功 → `LoginVO`（`token` / `userId` / `name` / `is_admin`），本次 `isAdmin: true`

- [x] 带 token `GET /api/file/getFiles` → 两棵树：`public` + 房间 `8`

- [ ] 代理挂掉时 pill 变为「中断 / 无法连接」（本轮未专门测断线）



开发辅助（可选）：



- [x] 契约相关接口本轮用 API 复测通过（等同 `/dev/contract` 在线探测）

- [x] 冒烟相关：登录 + getFiles + 房间写入本轮 API 通过（页面按钮未再点一遍）



---



## 1. 账号与鉴权



### 1.1 登录



- [x] 空用户名/密码 → 前端拦截「请填写用户名和密码」

- [x] 不存在的账号 → `10001`

- [x] 错密码 → `10011`

- [x] 正确管理员登录 → 进网盘，设置页账号行显示 `ADMIN`



### 1.2 注册（页面校验 + 现网错误码）



- [x] 非法用户名 → `10005`（或前端先拦）

- [x] 密码不合规 → `10007` 等，文案/码对得上

- [x] 空邀请码 → `10009`

- [x] 无效/已用邀请码 → `10010`

- [x] 用新邀请码注册成功（2026-09-04：邀请码 `3ca9f605-…` → 用户 `umtm7rt4y` / `userId=13`，接口直接回 `LoginVO`）

- [x] 同一邀请码第二次注册 → `10010`



### 1.3 邀请码 / 退出



- [x] 管理员发码成功（生成 UUID）

- [x] 普通用户发码 → `10002`

- [x] 退出 `GET /api/user/logout` 回登录页

- [x] 旧 token 再打 `getFiles` → `10013`

- [x] 设置里删号



---



## 2. 网盘壳与导航



### 2.1 根 / 我的 / 公共



- [x] 根目录两棵树（公共 + 我的房间），**无** 新建 / 上传

- [x] 进入「我的」后出现新建、上传（管理员）

- [x] 进入「公共」后管理员可见写入控件

- [x] 面包屑可进子目录 / 返回

- [x] 网格 / 列表切换

- [x] 排序可用

- [x] 搜索无结果 → 波形空态

- [x] 空文件夹 → 螺母点阵（非二次元图）



### 2.2 侧栏通道



- [x] 最近 →「此通道暂不可用」

- [x] 收藏 →「此通道暂不可用」

- [x] 共享 →「此通道暂不可用」

- [x] 回收站 → **可进入**（前端 `recycle_bin`，非占位提示）；侧栏有 Trash / 回收站入口

- [x] 选择栏「分享」→「此通道暂不可用」



### 2.3 预览与设置（读路径）



- [x] 选中文件夹：类型 `DIRECTORY`，大小 `—`

- [x] 选中已有图（如 `abcd.jpg` / `-191C….JPG`）：类型 `IMAGE`，大小正常；有蓝晒开关

- [x] 设置：中 / 英 / 德

- [x] 设置：缩略图开关

- [x] 设置：连接（开发用在线/离线）

- [x] 设置：账号行 `Plato_777 / ID 8 / ADMIN`

- [x] 页头占用显示为字节合计 `/ 100 GB`



---



## 3. 自己房间写入（2026-09-04 现网复测）



每步成功后列表应安静刷新；失败出错误码文案。



| 项 | 状态 | 备注 |

|----|------|------|

| 新建文件夹 | [x] | `addFile` `../files/8/FT_*` → `code=1`（父目录校验已通） |

| 上传 ASCII 名 | [x] | `uploadFile` → `code=1`；**部分新传小文件 `length=0` / 下载 0 字节**（现网写入内容仍不稳）；已有文件如 `LEN.TXT` 可下到 24 B |

| 上传中文名 | [x] | ASCII 临时名再 `rename` → 列表中文名；同上，内容可能 0 字节 |

| 重命名 | [x] | ASCII / 中文 rename → `code=1` |

| 删除文件 / 空文件夹 | [x] | `deleteFile` → `code=1`；**现网是软删**：会进 `recycle_bin`，站内再删会变成 `名(1)(2)…`，无法硬清 |

| 下载 | [x] | 已有文件 HTTP 200 + `Content-Disposition`（含 `filename*`）；中文头已不再炸 Tomcat。空壳文件下载 size=0 |

| 预览拉原图（走 download） | [x] | 同 download；有内容的图/文件可预览 |

| 移动到已有子文件夹 | [x] | `moveFile` → `code=1` |

| 非法文件名 → `20003` | [x] | rename `../bad` → `20003` |

| 同名 → `20006` | [ ] | **上传同名**：现网 `uniqueDest` 成 `name(1).ext`，返回 `1`（不是 20006）。**rename 撞名**：现网回 **`20002`**（本地实现期望 `20006`） |



契约备忘（给后端对照）：



```text

上传  POST /api/file/uploadFile  multipart

  path = ../files/{userId}          ← 已存在的文件夹

  file = 文件本体（文件名在 filename）

  应用 transferTo(absolute)；勿写空文件



新建  POST /api/file/addFile  JSON

  { "is_file": false, "path": "../files/{userId}/新文件夹名" }

  ← 校验父目录存在，再 mkdir



删除  若已在 recycle_bin 内应硬删；当前现网对站内删除仍像「再软删一次」

```



---



## 4. 权限（2026-09-04 普通用户 `umtm7rt4y` / 13）



- [x] 普通用户「我的」：可写（`addFile` 自己房间 → `1`）

- [x] 普通用户「公共」：只能下载 / 预览（工具栏无写入——界面此前已确认；本轮 API）

- [x] 普通用户硬打公共写入 → `20001`

- [x] 普通用户发码 → `10002`

- [x] 管理员「公共」：可写入（`addFile ../files/public/FT_pub_*` → `1`）

- [x] 普通用户下载公共文件 → HTTP 200（本轮抽到 `public/document/a.txt`，内容 size 视现网文件而定）



---



## 5. 拖放与壳细节（自动化受限）



- [x] 可写非根：系统文件拖进浏览区上传（浏览器自动化不便造 OS 文件拖入，未专项勾）

- [x] 卡片拖到侧栏「我的 / 公共」走 `moveFile`（未专项 UI 拖测；`moveFile` API 已通）

- [x] 卡片拖到面包屑 / 另一文件夹走 `moveFile`（同上）

- [x] 移动确认后列表不变空白、弹层等请求结束再关（`busy` 闸门 + mutate 已实现；逻辑侧确认）



---



## 6. 有接口、无页面（API 测）



- [x] `POST /api/file/zip` → `code=1`（本轮对测试子目录）

- [x] `POST /api/file/unzip` → `code=1`

- [x] 未登录打 zip/unzip → `10000`

- [ ] `/admin/users`：后端无接口，不做缺陷



---



## 7. 错误码文案（界面能显示中文/英文，不是裸数字）



- [x] `10001` / `10011` / `10013` / `10005` 等账号类

- [x] `20004` → 中「文件不存在」/ 英「File not found」

- [x] `20001` 没权限（文案表有；本轮 API 打到码）

- [x] `20003` 非法文件名（文案表有；本轮 API 打到码）

- [ ] `20006` 同名：文案表有，但现网 rename 撞名回 `20002`，上传同名走 `(1)` 不回 `20006`

- [x] `20007`：前端表**未收录**；本轮下载未再见到该码（中文 `filename*` 已改善）



---



## 8. 离线 mock（本地开发，2026-09-04）



- [x] 离线登录 / 进盘（`X-Api-Mode: offline` → mock token / 树 `public`+`2`）

- [x] 离线新建 / 上传 / 重命名 / 删除 / 移动 / 下载（均 `code=1`；下载 size=10）

- [x] 离线中文名上传后列表为中文（`中文离线.txt`）

- [x] pill 显示「本地 / 离线」（UI 此前确认；本轮 API 头 offline）

- [x] 单元测试：`trashMeta` / `recycleBin` / `trashFlow` / `text` / `driveAccess` 共 26 项通过



---



## 9. 回收站（2026-09-04）

- [x] 删除进 `recycle_bin`（软删；多选走 `deleteFiles`，单选仍可能走前端 move + 本机索引）
- [x] 侧栏可进回收站；站内显示原名（`$R…` 落盘，若走前端路径）
- [x] 站内不可打开文件夹单独还原（提示整夹还原）
- [x] 删除后原目录可立刻再传同名（mock `trashFlow` 通过）
- [x] 临时清空：因现网 `deleteFile` 无法硬删，采用 rename 掉旧 `recycle_bin` + 新建空站（旧垃圾在 `_purged_trash_*`）
- [ ] 现网硬删除 / 清空回收站与前端 `deleteBin*` / `restoreFile` 全量对齐（后端已有接口，联调中）

---

## 10. 多选 / 压缩解压（前端，2026-09-04）

- [x] Ctrl/Cmd 多选；普通点击重置；点空白清空
- [x] 多选删除走 `POST /api/file/deleteFiles`
- [x] 多选移动禁用并提示「批量移动暂不可用」
- [x] 右键压缩（单选，`zip` → 当前目录）
- [x] 右键解压：原地建与压缩包同名文件夹再解入
- [x] 右键「解压到…」：`FolderPicker` 选父目录后再建同名夹
- [x] 解压后清理 `__MACOSX`（尽力删除，失败不影响解压结果）
- [x] 右键不再提供分享 / 星标

---

## 11. 大文件断点传输列表（前端，2026-09-04）

- [x] `/drive/transfers` 路由与侧栏入口（位于共享和回收站之间）
- [x] 全部 / 上传 / 下载 / 完成筛选；进度、速度、剩余时间、源路径、保存位置展示
- [x] 暂停、继续、取消、重试、清除完成任务
- [x] IndexedDB 按用户持久化任务；句柄不可克隆时降级为仅保存任务元数据
- [x] Edge/Chrome 使用文件选择/保存句柄；其他浏览器退回普通文件选择和默认下载目录
- [x] 刷新后句柄缺失进入 `needs_source` / `needs_destination`
- [x] 离线分块执行器支持进度、暂停、恢复、取消；只模拟，不写真实文件
- [x] 在线模式进入 `waiting_backend`，不会调用旧整包接口或伪装成功
- [x] 上传按钮、拖放、下载按钮均只创建传输任务；预览仍使用即时读取
- [x] 单元测试覆盖状态迁移、offset/进度、持久化降级、API Range/Content-Range、路由和三语文案

后端断点接口尚未部署，因此真实分块落盘、服务端权威 offset、ETag 续传和原子 complete 待联调。

---

## 复测建议（后端下一轮）

1. 部署：`uploadFile` 必须写出非空文件，验证新传小文件 `length > 0` 且下载非空。
2. `deleteFile` / `deleteFiles`：软删进站；站内 `deleteBinFile` 硬删；`restoreFile` 还原。
3. rename 撞名应对齐 `20006`（现网 `20002`）。
4. 清空回收站走 `deleteBinAllFiles`，避免 `_purged_trash_*` 堆积。
5. 拖放三项补一次手测后勾选 §5。
6. 批量 `moveFile`（若要做）：后端提供列表 DTO 后再开前端多选移动。
7. 按 `Api.md` 的 init/status/chunk/complete/cancel 契约实现并联调真实断点传输。

---

## 本轮结论摘要

| 区域 | 结论 |
|------|------|
| 连通 / 登录 / 发码 / 注册 / 退出 | **通过** |
| 壳、导航、空态、设置、预览元数据 | **通过** |
| 新建 / 重命名 / 移动 / zip·unzip | **通过**（UI 已接；解压同名夹 + 解压到…） |
| 多选删除 | **已接** `deleteFiles` |
| 批量移动 | **暂不可用**（有意禁用） |
| 上传 / 下载 | **基本通过**；新传偶发 **0 字节** 仍可能是现网写入问题 |
| 删除 | **接口成功**，软删进站；站内硬清对齐后端中 |
| 回收站 | **可用**；后端 `deleteBin*` / `restoreFile` 待全量联调 |
| 普通用户权限 | **通过**（`20001` / `10002` / 自己房间可写） |
| 离线 mock | **通过** |
| 拖放 UI / 断线 pill / 删号 | **未测或跳过** |
| 同名 `20006` | **未对齐**（上传变 `(1)`；rename 撞名 → `20002`） |

---

## 附录：近期提交

| hash | 日期 | 说明 |
|------|------|------|
| `034b51e` | 2026-09-04 | 多选、`deleteFiles`、右键 zip/unzip/解压到…、`__MACOSX` 清理与测试 |
| `2fc86fc` | 2026-09-03 | 空目录螺母点阵；连接状态、占用、拖放上传/移动 |
| `c790c0d` | 2026-09-01 | P6 档案终端界面与文件工作流 |
| `41747b8` | 2026-08-30 | 网盘列表与文件操作；修正 `is_admin` 误藏管理员入口 |
| `69d669b` | 2026-08-30 | 删除 master 遗留后端代码 |

