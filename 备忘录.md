# 项目备忘

## 前端（已定）

- Vue 3 + Vite + TypeScript
- UI 组件：Element Plus（界面零件，不管文件格式）
- 不做 Electron（那是桌面壳；当前是浏览器访问网盘）
- 代码放 `frontend/`，和 Java 后端分开
- 开发时 Vite 将 `/api` 代理到 Spring Boot
- 个人目标与进度写在本地 `前端开发.md`（已 gitignore，不上传）

## 对接

- 接口契约先写在 `Api.md`，Swagger 后做
- JSON 字段驼峰，如 `inviteCode`
- 数据库：MySQL，之后直接接在服务器上

## 当前阶段

先做账号注册：`POST /api/user/register`  
DTO：`name`, `password`, `inviteCode`（字符串）  
返回：`boolean`（是否注册成功）
