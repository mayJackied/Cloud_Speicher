# 项目备忘

对外展示用仓库根目录 `README.md`。本文件给前后端对接，不展开给路人看的实现清单。
后续记得把测试工具找地方集合在目前的网站页面里；优化读取速度等问题

## 前端（已定）

- Vue 3 + Vite + TypeScript
- 网盘壳是自写档案终端（`archive.css`）；Element Plus 仍在依赖里，登录/网盘主界面不再铺 `el-container`
- 不做 Electron、不上 Tailwind、不加第二套 UI 库
- 代码放 `frontend/`
- 离线 mock；在线 Vite 代理到现网（不一定一直通）
- 个人进度：本地 `前端开发.md`（gitignore）
- 后端本地目录 `backend/`（gitignore）

## 对接（backend `origin/backend`，含回收站 API）

- 契约：仓库 `Api.md` + `conteact/Api.md` + `conteact/ErrorCode.md`；**以 Java 为准**
- `Result { code, data }`，没有 `msg`。成功 `code=1`；失败 `code` 为整数错误码，前端 `messageForCode` 查表
- JSON 大体驼峰；`is_admin` / `is_available` / `is_file` / `new_name` 按 Jackson 蛇形发/收
- no jwt：`checkUserName`、`login`、`register`
- 其余请求头 `token`；**用户 id 只从 JWT 取**，发码/删用户/文件列表都不要在 DTO 里塞 userId
- LoginVO：`token, userId, name, is_admin`
- JWT 有效期 30 天；**不做 refresh**。过期 `10000`；`GET /api/user/logout` 或删号后 token 进黑名单，再请求 `10013`
- 请求 JSON 已加 `@RequestBody`（上传是 multipart）
- 文件：`deleteFiles`（批量软删）、`zip` / `unzip`、`moveFile`；回收站 `deleteBinFile` / `deleteBinAllFiles` / `restoreFile`

## 当前阶段

P7 移动可用；已接 **Ctrl/Cmd 多选**、批量 `deleteFiles`、右键 **压缩 / 解压 / 解压到…**（同名文件夹 + 可选父目录，清 `__MACOSX`）。侧栏回收站可进。批量移动暂不可用（后端无批量 move）。最近 / 收藏 / 共享仍占位。细节见本地 `前端开发.md`。

连上真实服务器后要测什么，见下文「现网联调测试清单」；**带勾选的完整功能测试结果**见仓库根目录 `功能测试.md`（最近一轮：**2026-09-04** 现网 + 离线复测；同日补文档对齐多选与压缩解压 UI）。

**正式版：** 登录页（以及注册页同一套）的离线 / 在线模式选择要删掉。那是开发用 mock 开关，不能留到对外发布。设置 → 连接 里的切换一并拿掉或只留开发构建。发版默认只打真实后端。

---

## 现网联调测试清单（连上服务器之后）

默认开发页是 **离线 mock**。测真机前：登录页或设置 → 连接，切到「在线」（`localStorage apiMode = online`，请求头 `X-Api-Mode: online`）。右上角 pill 会显示离线 / 在线 / 无法连接，跟着实际请求走，**没有定时探测**。Vite 会把 `/api` 代理到 `8.130.215.175:8080`。代理连不上会报 `ECONNREFUSED` / proxy error，pill 变成「无法连接」。先确认 FRP 通了再测功能。

> **发版备注：** 登录页 / 注册页的「离线 | 在线」是开发开关（`ApiModeSwitch`），**正式版推出后删除**。用户不应在登录时选择 mock。设置里的「连接」分区同样是开发用，发版时去掉或仅 `import.meta.env.DEV` 显示。

开发页可先跑一遍：`/dev/contract`、`/dev/smoke`。主路径仍以 `/login` → `/drive` 手测为准。

### 0. 通了没有

- 未登录打 `POST /api/user/checkUserName`：HTTP 200，`Result`，`data.is_available` 真假都对（不要再出现 400）
- 登录成功：`code=1`，`data` 是 LoginVO（`token` / `userId` / `name` / `is_admin`）
- 带 `token` 头打 `GET /api/file/getFiles`：两棵树，`public` + 自己的房间目录（文件夹名 = 用户 id）

### 1. 账号

用 **普通用户** 和 **管理员** 各走一遍。

- 查用户名占用 / 可用
- 邀请码注册成功，自动进盘；同一码再注册别人 → `10010`
- 非法用户名、密码（长度、无数字、含符号）→ `10003–10008`，文案对得上
- 错密码 → `10011`；不存在的账号 → `10001`
- 普通用户打开邀请码页发码 → `10002`；管理员发码成功，新码能注册
- 退出：`GET /api/user/logout` 成功后回登录页；同一 token 再请求 → `10013`，被踢回登录
- 设置里删号：成功后 token 作废；该用户名应可再注册（若后端允许）

**已知坑：** Java 用 BeanUtils 拷 LoginVO 时，`is_admin` 现网可能一直是 `false`。管理员登录后看设置页账号行、公共目录能否写入。若写不了，先看登录 JSON 里到底有没有 `is_admin: true`。邀请码注册成功 / 发码 `10002` 可侧面判断角色。

### 2. 列表与权限（根 / 我的 / 公共）

- 根目录能看到两棵树，**不能** 新建、上传、下载、移动（根不可写、不可下）
- 「我的」= 自己的 `userId` 房间：可新建文件夹、上传、重命名、删除、移动、下载、预览；Ctrl/Cmd 多选
- 「公共」普通用户：**只能下载 / 预览**，工具栏写入按钮应不可用；硬打写入接口应 `20001`
- 「公共」管理员：可增删改上传
- 进子文件夹、面包屑返回，刷新后仍在当前目录（或至少能再点进去）
- 侧栏「回收站」可进；「最近 / 收藏 / 共享」和顶栏「分享」仍提示通道不可用（右键已去掉分享/星标）

### 3. 自己房间里的文件操作（优先测）

每一步成功后列表应 **安静刷新**（不要整页 Loading 把网格卸掉），失败要出错误码文案。

| 操作 | 期望 | 特别盯 |
|------|------|--------|
| 新建文件夹 | 出现在当前目录 | 现网 `addFile` 要求 **父路径已存在**；根下直接建可能 `20004`。先进入自己的房间再新建 |
| 上传 ASCII 名 | 当前目录出现该文件 | multipart，`path` 是 **目标文件夹**；不要带 JSON Content-Type |
| 上传中文名 | 列表显示中文，不是 `upload_xxxx` | 前端会先 ASCII 别名再 `renameFile`；若只剩英文临时名，说明 rename 在现网失败 |
| 列表里已有乱码名 | 尽量显示成中文 | 只能修 UTF-8 被当成 Latin-1/CP1252 的那种；别的乱码只能人手改名 |
| 重命名 | 新名出现，旧名消失 | `new_name` 蛇形；非法名（`.` `..` 含 `/` `\`）→ `20003` |
| 删除文件 / 空文件夹 | 进回收站（软删） | 多选走 `POST /file/deleteFiles`（body 为 `{path}[]`）；单选仍可能走前端软删路径，以现网为准 |
| 下载 | 浏览器拿到原文件，中文文件名不要再乱 | 成功是二进制流，不是 Result；失败才是 JSON `code` |
| 预览 | 图/文能出；GIF/视频是静止帧 | 蓝晒开关只改预览着色，不影响下载原文件 |
| 移动到已有子文件夹 | 源目录没了，目标里有了 | **仅单选**；多选点移动应提示暂不可用。`targetDir` 须已存在，否则 `20004`；同名 → `20006` |
| 压缩（右键） | 当前目录出现 `.zip` | `zip`：`{ path, targetDir }`，单选 |
| 解压 / 解压到…（右键） | 出现与压缩包同名的文件夹，内容在里面 | `unzip` 的 `targetDir` 为该文件夹路径；「解压到…」先选父目录。解压后不应残留 `__MACOSX` |

**移动补测：** 把文件从自己房间的 A 夹挪到 B 夹；不要挪到 `public`（权限）；不要把文件夹挪进自己内部。确认后若长时间转圈，看网络面板是不是 `moveFile` 一直 pending 或 500。

### 4. 错误码对照（手测时顺手记）

应能在界面上看到对应中文，而不是 raw number：

- `20001` 没权限（普通用户改公共区）
- `20003` 非法文件名
- `20004` 路径不存在（新建/移动目标文件夹尚未创建时最常见）
- `20006` 同名
- `10000` / `10013` 清会话回登录（过期或已退出）

### 5. 有接口、页面部分已接 / 仍待对齐

- `POST /api/file/zip`、`/file/unzip`：DTO `{ path, targetDir }`。**页面已接右键**；解压默认同名文件夹，「解压到…」用 `FolderPicker`
- `POST /api/file/deleteFiles`：批量软删；多选删除已接
- 回收站后端：`deleteBinFile` / `deleteBinAllFiles` / `restoreFile`（前端侧栏可进站；与后端 API 全量对齐仍可继续联调）
- 收藏、共享、最近：**没有后端**，测到「通道不可用」即可
- `/admin/users` 用户列表：**没有后端**，不要当缺陷开给前端
- **批量移动**：后端尚无列表接口，前端多选时禁用并提示

### 6. 建议顺序（两人联调）

1. 代理通 → checkUserName / login / getFiles  
2. 普通用户：自己房间新建 → 上传英文 → 上传中文 → 重命名 → 移动 → 下载 → 删除  
3. Ctrl 多选 → 批量删除；单选右键压缩 → 解压 / 解压到…（确认无 `__MACOSX`）  
4. 同一用户进公共区：只能下，不能改  
5. 管理员：发邀请码 → 公共区写一次 → 看 `is_admin` 是否为真  
6. 退出后再用旧 token 打 getFiles，确认 `10013`

### 7. 空目录 / 拖放（前端壳，现网顺手看一眼）

- 空文件夹中间是螺母点阵（`glyphs/nut.png` 采样），不是二次元图；刷新或再进可能换一套字符：方块 ` ░▒▓█` / 极客 ` .:+*#X%` / 点阵 ` .·:;:i1tfLCG08@`
- 搜索无结果仍是波形字
- 可写且非根：把系统文件拖进浏览区应能上传；卡片拖到侧栏「我的 / 公共」、面包屑或另一文件夹应走 `moveFile`（公共区仅管理员可写）
- 页头「已占用」是当前树字节合计，空为 `0 B`，上限仍写死 `/ 100 GB`（以后再改）

测完把 **失败的接口、请求 JSON、返回 `code`、是否 20004/is_admin 仍假** 记在本文件当天日期下面，方便对照 Java。

## 用户名 / 密码

后端已校验，与前端一致：

- 用户名：3–20，字母开头，字母数字下划线
- 密码：8–64，须有字母和数字，且只能字母数字
- 确认密码只前端
- 邀请码非空（UUID 字符串）

## 布尔命名

Java PO：`isDeleted` / `isAdmin`。JSON VO 部分是 `is_admin`、`is_file`、`is_available`，前端类型仍用驼峰，读写时做映射。

## Document

后端已删 `Document.java`。不要再对接 `documentId`。房间目录 = 配置路径 + JWT `user_id`。

## 文件名（rename）

`new_name` 非空、不是 `.` / `..`、不含 `/` 或 `\`。前端 `isLegalFileName`。

## wasm-hasher

`pkg` 需 wasm-pack。Hasher 开发页走 Web Crypto，不要从 App.vue 静态引缺失的 wasm。

## JwtFilter（`deb43ee`）

白名单：`/checkUserName`、`/login`、`/register`、swagger。空 token → `10000` 并 return。黑名单命中 → `10013`。JWT 声明 `user_id`。`logout` / `creatInviteCode` 都要 token。

## 2026-08-29

同步 backend 至 `67d1c95`：Result 去 msg、错误码表、checkUserName、GET 发码、文件 API 文档与 api 层。

## 2026-08-30

同步 backend 至 `0555821`：zip / unzip / moveFile；AddFileDTO 改名为 FileDTO（JSON 仍是 `is_file` + `path`）；错误码 20005 / 20006。登录注册发码未改。`pom.xml` 增加 `commons-compress` 1.28.0。

仓库提交：`69d669b` 删 master 遗留后端；`41747b8` 接上网盘列表与文件操作，修正登录 JSON `is_admin` 误藏管理员入口（自己房间可写；公共区普通用户只下载）。

在线联调（FRP `8.130.215.175:8080`）已通：

- `checkUserName` HTTP 200，`is_available` 真假都对（此前 400 已不在）
- `register` / `login` 成功为 `LoginVO`；邀请码一次性，用过再注册 → `10010`
- 非管理员 `creatInviteCode` → `10002`
- 带 token 的 `getFiles` → public 树 + 自己的房间目录
- JWT 30 天、无 refresh；前端 `sessionStorage` 关标签即登出，到期再登录即可

P3 `/drive`：列表、面包屑、进文件夹；新建/重命名/删除（名称在页面输入，不用 prompt）。上传当前目录、文件行下载。公共目录普通用户只能下载，管理员可写。上传必须是 multipart（不要带 JSON Content-Type），path 同时放表单字段和 query。后端 `addFile` 的权限检查要求 path 已存在，现网新建文件夹可能 `20004`（mock 允许新建）。上传接口的 `path` 是目标文件夹。

## 2026-08-31

同步 backend 至 `deb43ee`：普通用户可下载公共区；`GET /api/user/logout`；JWT 黑名单错误码 `10013`。前端退出已接（待提交）。

P6 网盘壳已出门（不是当初那版 Element Plus 后台布局）。下一步：zip / unzip 页面；回收站、`/admin/users` 等后端。完整清单在 `前端开发.md`。

## 2026-09-01

提交 `c790c0d`（feat(ui/drive): complete Stage P6 archive terminal interface and file workflow）。

网盘主界面按档案终端收完一版：常驻选择栏、中日韩文件名、设置分区、展示用根 README。随后修了 move：确认后不再把列表卸成 Loading 空白；离线 mock 真正搬家；弹层等到请求结束再关。下一步仍是 zip/unzip 页面。

补了「现网联调测试清单」（见上文）：连上 FRP 之后按账号、权限、房间 CRUD、中文名、移动、错误码的顺序测；zip/unzip 和回收站等仍无页面。

## 2026-09-03

提交 `2fc86fc`（网盘空目录改回六角螺母点阵…连接状态、占用显示、拖放上传/移动）。

右上角「稳定 / 在线」不再写死。离线 mock 显示「本地 / 离线」；切到在线后，最近一次请求打到后端显示「稳定 / 在线」，代理挂了显示「中断 / 无法连接」。不额外轮询。登录页和设置 → 连接 可切换。

**正式版要删：** 登录页（注册页同组件）的离线 / 在线选择；设置 → 连接 也是开发开关。发版后只连真实后端。

预览「大小」不再把类型写进大小栏：文件用 `length`，文件夹加总当前树子文件；空文件夹为 `—`。页头存储 / 同步 / 状态同一行；占用显示 `formatBytes(usedBytes)`（空 `0 B`）`/ 100 GB`。

空目录：透明底六角螺母 PNG（`frontend/src/assets/glyphs/nut.png`）按块采样成 ASCII，三套灰度表随机。搜索无结果仍用手写波形。二次元线稿方案已撤回。

浏览区可拖系统文件上传（可写且非根）；卡片可拖到侧栏 / 面包屑 / 文件夹卡片做 `moveFile`。回收站侧栏可进。zip / unzip 已接右键（见 2026-09-04）。

### 现网手测（管理员 Plato_777 / userId 8，在线，FRP 通）

通过：

- `checkUserName` HTTP 200；`Plato_777` → `is_available: false`，新名 true
- 登录 `isAdmin: true`（这次不是假的）；pill「稳定 / 在线」
- 登录空表单「请填写用户名和密码」；不存在用户 `10001`；错密码 `10011`
- 注册页前端拦截非法用户名 / 空密码 / 空邀请码；现网 `10005` / `10007` / `10009` / `10010` 文案或码对得上
- 根：两棵树（公共 + 我的房间 8），无新建/上传
- 我的 / 公共（管理员）有新建、上传；面包屑、网格/列表、排序、搜索无结果波形空态、空文件夹螺母点阵
- 侧栏最近/收藏/共享/回收站、选择栏分享 →「此通道暂不可用」
- 预览元数据：文件夹 DIRECTORY / `—`；`abcd.jpg` 为 IMAGE / 16 KB（缩略图仍是 IMG 占位，蓝晒钮在）
- 发码成功（已生成一条 UUID 邀请码）；设置：中/英/德、缩略图开关、连接、账号行 `Plato_777 / ID 8 / ADMIN`
- 退出后旧 token 打 `getFiles` → `10013`；未登录 zip/unzip → `10000`

失败（现网写入/下载）：

- `POST /api/file/addFile` `{"is_file":false,"path":"../files/8/E2E_A"}` → `20004` 文件不存在（UI 已显示）
- `POST /api/file/uploadFile?path=../files/8/recycle_bin` multipart → `20004`（父目录已存在仍失败）
- 公共区下载 `a.txt` / 预览图走同一 download → **`20007`**，前端错误表没有此码，界面显示「错误码 20007」
- 因此本轮没能测完：中文名上传、rename、move、delete 成功路径、普通用户公共只读

未测：拖放上传/移动（浏览器自动化限制）；删号（会毁掉管理员）。

## 2026-09-04

提交 `034b51e`（Add Ctrl multi-select, batch deleteFiles, and zip/unzip with extract-to picker.）：

- Ctrl/Cmd 多选；批量删除 → `deleteFiles`；移动仅单选（多选提示暂不可用）
- 右键压缩 / 解压 / 解压到…；解压进同名文件夹；成功后清 `__MACOSX`
- 软删/还原相关前端路径与自动化测试
- 右键去掉分享、星标；顶栏仍保留分享/星标占位提示
- 文档：`README.md` / `Api.md` / 本备忘录 / `功能测试.md` 已对齐上述行为

### 近期 git 提交（对照）

| hash | 日期 | 说明 |
|------|------|------|
| `034b51e` | 2026-09-04 | 多选、`deleteFiles`、右键 zip/unzip/解压到…、`__MACOSX` 清理与测试 |
| `2fc86fc` | 2026-09-03 | 空目录螺母点阵；连接状态、占用、拖放上传/移动 |
| `c790c0d` | 2026-09-01 | P6 档案终端界面与文件工作流 |
| `41747b8` | 2026-08-30 | 网盘列表与文件操作；修正 `is_admin` 误藏管理员入口 |
| `69d669b` | 2026-08-30 | 删除 master 遗留后端代码 |

### 断点传输后端对接注意事项

- UUID 会话必须绑定 `userId`、方向、源/目标路径、文件名、总长度、chunkSize、哈希、状态与 TTL；不能只拿 UUID 当裸 key。
- 数据库只保存会话、权威 `nextOffset` 或已收区间；文件字节写 `.partial` 临时文件/对象存储。
- v1 采用严格顺序分块。服务端校验 `Content-Range.start === nextOffset`；同 offset+同哈希重试幂等，不同内容返回冲突。
- init 带 `clientUploadId` 并建立用户范围唯一索引，防止网络重试生成多个会话。
- complete 校验总长度和 SHA-256 后原子 rename；未完成文件不能进入 `getFiles`。
- cancel 与定时任务都要删除临时内容；完成、取消、过期接口均应幂等。
- 下载优先标准 `Range` / `206` / `ETag` / `If-Range`，通常无需把每个下载 offset 频繁写数据库。
- 传输接口均验 token 和 transferId 所属用户；公共区继续执行读/写权限与配额检查。
- 前端在线模式在接口未部署前显示“等待后端支持”，不会回退旧整包接口或伪装传输成功。
