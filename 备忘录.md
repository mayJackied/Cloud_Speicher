# 项目备忘

## 前端（已定）

- Vue 3 + Vite + TypeScript
- UI 组件：Element Plus（界面零件，不管文件格式）
- 不做 Electron（那是桌面壳；当前是浏览器访问网盘）
- 代码放 `frontend/`，和 Java 后端分开
- 离线：本地 mock；在线：Vite 代理到 `8.130.215.175:8080`（FRP）
- 个人目标与进度写在本地 `前端开发.md`（已 gitignore，不上传）
- 后端分支本地目录 `backend/`（gitignore，不随前端上传）

## 对接

- 接口契约：`Api.md`（中英）+ backend `conteact/`
- DTO 前端→后端，VO 在 Result.data 里；`code` 1 成功 / 0 失败
- JSON 驼峰，如 `inviteCode`
- login / register / getUsersName：no jwt
- 其余请求：请求头 **`token`**（`LoginVO.token`），不是 Bearer
- 登录/注册成功 VO 现为 `LoginVO { token, userId, name, isAdmin }`，不再是 UserVO

## 当前阶段

注册 + 登录看 `Api.md` 状态表（code/msg/data）。会话存 `LoginVO`，请求头 `token`。没有 refresh。
成功后进 `/drive`。管理员可开 `/admin/invitations` 发码。

## 响应形态（不要退回 boolean）

早期可以 `return: boolean` 把注册打通，但 `false` 信息量不够：用户名已存在、邀请码无效/已用/过期/撤销、参数空、服务器错误，前端只能一律显示「注册失败」。

当前已经用 `Result { code, msg, data }`，**不要再改回 boolean**。

以后（不强迫现在改）可再升业务码，方便 Vue `switch`：

```text
REGISTER_SUCCESS / USERNAME_EXISTS / INVALID_INVITE_CODE
INVITE_CODE_USED / INVITE_CODE_EXPIRED / INVITE_CODE_REVOKED / INVALID_PARAMETER
```

例如 `{ success, code: "USERNAME_EXISTS", message }`。等注册联调跑通再升；先把 HTTP 状态码约定好（见 `Api.md`）。

Axios 会按 HTTP 进 `try`/`catch`。如果业务失败也一律 `200 + false/code=0`，以后接口多了会很难拆。约定：

- 成功：`200` + Result `code=1`
- 用户名冲突：`409` + Result `code=0`
- 邀请码/参数问题：`400` + Result `code=0`
- 服务器错误：`500`

前端两种都接：`200 + code=0`（现有 Java）和 `4xx + code=0`（约定目标）。

## 用户名 / 密码约束

前端先拦，后端应对齐。

- 用户名：不能为空；**3–20 位**，字母开头，只能字母、数字、下划线
- 密码：不能为空；**8–64 位**，须同时包含字母和数字
- 确认密码：必须与密码一致（只在前端，不进 DTO）
- 邀请码：不能为空

## 布尔字段命名

`is` + 状态，过去式/形容词，不要 `isDelete`：

```text
isDeleted
isAdmin
isActive
```

前端登录态用 `LoginVO`，不再读 `isDeleted`。PO 仍有删除标记；Java 若暂时仍是 `isDelete`，只做兼容映射，新字段按这个规矩起名。

## Document（P3 再接）

Java `Document` 已有 `documentName`、`documentSize`。文件列表接口还没有，前端先不实现。

## wasm-hasher

`wasm-hasher/pkg` 要 `wasm-pack build` 才有。本机若没装 Rust，不要从 `App.vue` 静态引用它，否则 Vite 整站红屏。Hasher 开发页走浏览器 SHA-256。

## 当前 Java 过滤器（后端修；前端按契约，不当已修好）

`JwtFilter` 现状（62d048c）：

- 白名单是 `/creatInviteCode`、`/login`、`/register`，命中后 **直接 return，没有 `filterChain.doFilter`**，这三条可能到不了 Controller
- 契约里 **no jwt** 的 `getUsersName` 不在白名单；缺 header 时 `jwt` 为 null 再 `isEmpty()` 可能 NPE
- 缺 token 时写了 `NOT_LOGIN` 仍继续 `doFilter`
- 契约要求发码带 `token`，过滤器却把 `creatInviteCode` 放进了白名单

前端仍按契约：login/register/getUsersName 不带头；发码带头 `token`；收到 `NOT_LOGIN` 清会话（这三条 no-jwt 接口除外，避免注册页被误踢）。

Controller 方法仍没有 `@RequestBody`，在线 JSON 绑定可能失败。没有 refresh。

## 2026-08-28 联调记录（前端探测）

- 离线：register / login 通过；冒烟通过（Result + LoginVO）
- 离线发码：默认会话若不是 `admin`，会得到 `code=0 msg=您不是管理员`（形状正确的失败用例）。成功路径：用户名 `admin` 先 login 再发码
- 在线：契约页三条都是 HTTP 500；冒烟曾显示「未连通」。500 表示代理有响应，和超时不是一回事。可能原因：FRP/上游 500、`JwtFilter` 白名单直接 return、缺 `@RequestBody` 导致 NPE。前端按契约继续等后端修，不把 500 当本地失败

## Register.vue

```text
el-form
├── 用户名
├── 密码
├── 确认密码
├── 邀请码
└── 注册按钮
```

流程：

```text
点击注册
    ↓
前端校验（空值、格式、两次密码一致）
    ↓
POST /api/user/register   DTO 只有 name / password / inviteCode
    ↓
Result { code, msg, data }（不要再当 boolean）
    ↓
ElMessage.success / ElMessage.error(msg)
    ↓
code=1 进入 /drive
```


